@model IEnumerable<Category>

<li class="dropdown">
    <a asp-controller="xProduce"
       asp-action="Index"
       class="dropdown-toggle" data-toggle="dropdown">Produce List<b class="caret"></b></a>
    <ul class="dropdown-menu">
        @foreach (var category in Model)
        {
            <li>
                <a asp-controller="Produce" asp-action="ProduceList"
                   asp-route-category="@category.CategoryName">@category.CategoryName</a>
            </li>
        }
        <li class="divider"></li>
        <li>
            <a asp-controller="Produce" asp-action="ProduceList" asp-route-Category="">View all items</a>
        </li>
    </ul>
</li>